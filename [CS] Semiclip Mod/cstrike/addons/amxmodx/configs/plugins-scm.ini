; rename this file to disabled-scm.ini to turn it off.
; rename it back to plugins-scm.ini to turn it on.

; put a semi-colon in front of a plugin to disable it.
; add the word debug after a plugin to place it in debug mode
; for example: scm_entity_editor.amxx debug
; remove a semi-colon to re-enable a plugin

scm_entities_editor.amxx
scm_entities_fix.amxx

;scm_traceline_fix.amxx ; Traceline Fix (Deathrun)
scm_main.amxx
